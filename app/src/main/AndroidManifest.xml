<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lauren.simplenews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.widget.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".MainListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                  <action android:name="android.intent.action.MAIN" />

                  <category android:name="android.intent.category.LAUNCHER" />
              </intent-filter>
        </activity>
        <activity
            android:name=".news.widget.NewsDetailActivity"
            android:label="@string/title_activity_news_detail"
            android:theme="@style/AppTheme.NoActionBar.Translucent"></activity>
        <activity
            android:name=".DrawerLayout.DrawerLayoutTest"
            >
        </activity>

        <activity
            android:name=".MainRecycleViewActivity"
            >
        </activity>
        <activity
            android:name=".recyclerview.RecyclerViewTest"
            >
        </activity> <activity
        android:name=".okhttp.OkhttpTestActivity"
        >
    </activity> <activity
        android:name=".okhttp.OkHttpGetActivity"
        >
    </activity> <activity
        android:name=".okhttp.SnakeBarTest"
        >
    </activity> <activity
        android:name=".async.TestAsyncTaskActivity"
        >
    </activity>

    </application>

</manifest>
